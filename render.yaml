services:
  - type: web
    name: MCP-Server
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: CFT_BASE_URL
        value: https://cft-terminal-hub.vercel.app
      - key: NODE_ENV
        value: production
